# 安全策略与公众监管基线

[English](SECURITY.md) | **简体中文**

## 项目安全定位

本项目实现生成式AI驱动的本地网页重构。为保护公众用户权益和范式公信力，所有安全机制开放、可审计、接受社区监管。

## 核心安全基线（硬编码 & 公开锁定）

### 1. 能力分层

- **安全默认模式**：仅解析并执行AI生成的CSS，丢弃所有JS代码。
- **高级危险模式**：用户手动激活 + 显著红色风险警告，责任由用户自行承担。

### 2. 数据脱敏机制

上传AI前自动过滤所有敏感DOM节点：

- 密码输入框（`input[type="password"]`）
- 隐藏表单值
- 用户隐私表单数据（信用卡、身份证号等）

### 3. 本地隐私保护

- 用户API Key仅存储在浏览器本地存储（`chrome.storage.local`）中。
- 无后端数据收集，无偷传行为。
- 纯本地运行，无远程数据上报。

### 4. CSP严格限制

- 遵循Manifest V3标准严格内容安全策略。
- 禁止远程动态脚本注入。
- 所有可执行代码必须打包在扩展内，禁止远程加载代码。
- 消除后门远程控制风险。

### 5. 可审计性

- 保留本地行为日志供用户自审（DOM上传、API调用、代码执行记录）。
- 所有核心逻辑开源，接受全面公众审计。
- 发布版本提供源码哈希供校验。

## 漏洞披露

- 通过GitHub Issues公开渠道报告安全漏洞。
- 所有安全修复公开披露并记录在 [CHANGELOG.md](CHANGELOG.md) 中。
- 报告者将在安全公告中署名致谢。

## 风险免责声明

本范式是公共开放的技术探索。高级手动开启的AI动态代码执行模式具有固有风险。所有使用行为均为用户的自主选择。作者和社区贡献者不承担相应责任。完整免责声明见GNU通用公共许可证第3版第15、16条。
